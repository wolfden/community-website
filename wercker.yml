box: samueldebruyn/debian-git
build:
  steps:
    - arjen/hugo-build:
         flags: "--destination='deploy' --verbose=true"
deploy:
  steps:
    - ysqi/git-push@0.7.15:
         gh_oauth: $GIT_TOKEN
         repo: Sabayon/community-website
         gh-pages: true
         basedir: deploy
